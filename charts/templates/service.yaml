apiVersion: v1
kind: Service
metadata:
  name: {{ include "time-machine-smb.fullname" . }}
  labels:
    {{- include "time-machine-smb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
      name: {{ .name }}
    {{- end }}
  selector:
    {{- include "time-machine-smb.selectorLabels" . | nindent 4 }}
